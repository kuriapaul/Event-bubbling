import { StopEventBubblingDirective } from './stop-event-bubbling.directive';

describe('StopEventBubblingDirective', () => {
  it('should create an instance', () => {
    const directive = new StopEventBubblingDirective();
    expect(directive).toBeTruthy();
  });
});
